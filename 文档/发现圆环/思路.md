1,在图片中在截出一个矩形（可以识别出圆环），找出圆环凸点（与赛道边缘最近的点），找出凸点水平方向上与赛道的交点1
2，找到赛道在图片最下方的最先扫描出来的左点1，右点1，两点相连，找到中点（中点2）
3，将圆环凸点和交点两个点连线，找出中点（中点1），与上面中点相连，找到条中线
4，在中点1上方（中线上）找到一个点（在入圆环的口那），向左扫能扫到一条最长白列1（远大于中点1与交点1的连线）
5，同理，在中点1下方（中线上）找到一个点（在出圆环的口那），向左扫能扫到一条最长白列2（大于白列1）
条件：
扫描最初的左点1，右点

if(找到圆环凸点)：
            x中点1=(x(交点)+x(凸点))/2
            y中点1=y(交点)
            x中点2=(x(左点)+x(右点))/2
            y中点2=y(左点)
            将两点连成一条中线
            中线上取一点，在中点上方取合适的入口点1，找到坐标（x1,y1）
            向右扫，直到扫到黑点，相连，得到一条白列1
            向左扫，直到扫到黑点，相连，得到一条白列2
            中线上取一点，在中点下方取合适的出口点2，找到坐标（x2,y2）
            向右扫，直到扫到黑点，相连，得到一条白列3
            向左扫，直到扫到黑点，相连，得到一条白列4
            if(白列1长度<白列2长度):
                if(白列3长度<白列4长度):
                    if(白列2长度<白列4长度):
                    发现圆环






